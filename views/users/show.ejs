<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= user.username %>'s Pantry</title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>

  <h1><%= user.username %>'s Pantry</h1>

  <% if (user.pantry.length > 0) { %>
    <ul>
      <% user.pantry.forEach(item => { %>
        <li><strong><%= item.name %></strong> — Quantity: <%= item.quantity %></li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>This user hasn't added any pantry items yet.</p>
  <% } %>
</body>
</html>