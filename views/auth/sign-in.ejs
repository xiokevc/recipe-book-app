<!DOCTYPE html>
<html lang="en">
<head>
  <link href="https://fonts.googleapis.com/css2?family=Dancing+Script&display=swap" rel="stylesheet">
  <meta charset="UTF-8">
  <title>Welcome</title>
  <link rel="stylesheet" href="/public/styles.css">
  <style>

body, html {
  height: 100%;
  margin: 0;
  font-family: 'Arial', sans-serif;
  background: url('https://media1.giphy.com/media/xUA7bjfr8Q1BB3LbJm/source.gif') no-repeat center center fixed;
  background-size: cover;
  display: flex;
  justify-content: center;
  align-items: center;
}

.container {
  width: 360px;
  height: 640px;
  background: white;
  border-radius: 24px;
  box-shadow: 0 20px 40px rgba(0,0,0,0.15);
  overflow: hidden;
  position: relative;
  border: 3px solid black; /* thin black border */
}

.login-container {
  position: relative;
  height: 100%;
  padding: 60px 32px 32px 32px;
  box-sizing: border-box;
}

h1 {
  position: relative;
  color: rgb(0, 0, 0);
  font-weight: 800; /* Stronger weight */
  font-size: 4rem; /* Bigger text */
  z-index: 1;
  margin: 0 0 48px 0;
  font-family: 'Dancing Script', cursive; /* Or 'Brush Script MT', cursive */
  text-align: center;
  text-shadow: 1px 1px 2px rgba(0,0,0,0.2); /* optional: more pop */
}

.login-form input {
  display: block;
  width: 100%;
  margin-bottom: 24px;
  border: none;
  border-bottom: 1px solid #bbb;
  padding: 8px 4px;
  font-size: 1rem;
  outline: none;
  transition: border-color 0.3s;
  z-index: 1;
  position: relative;
  background: transparent;
}

.login-form input:focus {
  border-bottom-color: #543257;
}

.btn-signin {
  display: flex;
  align-items: center;
  justify-content: center;
  background: #000000;
  border: none;
  border-radius: 6px; /* slightly rounded corners */
  width: 120px; /* longer width */
  height: 40px; /* slightly shorter height for rectangular look */
  cursor: pointer;
  color: white;
  font-weight: 600;
  font-size: 1rem;
  transition: background 0.3s ease;
  position: relative;
  margin-top: 8px;
  z-index: 1;
  padding: 0 12px;
  text-align: center;
}

.btn-signin:hover {
  background: #c5517c;
}

.btn-signin .arrow {
  margin-left: 4px;
  font-weight: 900;
  font-size: 0.9rem;
}

.bottom-links {
  display: flex;
  justify-content: space-between;
  margin-top: 48px;
  font-size: 0.9rem;
  position: relative;
  z-index: 1;
}

.bottom-links a {
  text-decoration: underline;
  color: #333;
  cursor: pointer;
}

.logo {
  display: block;
  margin: 0 auto 16px auto; /* centers logo and adds spacing below */
  width: 150px;              /* adjust size as needed */
  height: auto;             /* keeps aspect ratio */
}
  </style>
</head>
<body>
  <div class="container">
    <div class="login-container">
      <div class="top-shape"></div>
      <img src="/assets/forkit.png" alt="Logo" class="logo" />
      <h1>Fork it </h1>
      <form class="login-form">
        <input type="email" placeholder="Email" required />
        <input type="password" placeholder="Password" required />
        <button type="submit" class="btn-signin">
          <strong>Sign in</strong>
          <span class="arrow">→</span>
        </button>
      </form>
      <div class="bottom-links">
        <a href="/auth/sign-in">Register</a>
        <a href="/forgot-password">Forgot Password?</a>
      </div>
    </div>
  </div>

  <script>
    //add JS for interaction (like toggling sign up and forgot password)
  </script>
</body>
</html>