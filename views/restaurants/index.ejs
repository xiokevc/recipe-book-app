<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Your Restaurant Reviews</title>
  <style>
    :root {
      --bg: #fff;
      --text: #333;
      --accent: #e07a5f;
      --max: 1200px;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
      color: var(--text);
      background: var(--bg);
      line-height: 1.6;
    }

    a {
      color: var(--accent);
      text-decoration: none;
    }

    .site-header {
      background: var(--bg);
      border-bottom: 1px solid #ddd;
      padding: 1rem;
      max-width: var(--max);
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .site-header ul {
      list-style: none;
      display: flex;
      gap: 1rem;
    }

    .site-header h1 {
      font-size: 1.5rem;
    }

    .hero {
      text-align: center;
      padding: 4rem 1rem;
      background: #f5f5f5;
    }

    .hero h2 {
      font-size: 2.5rem;
      margin-bottom: 1rem;
    }

    .hero .btn {
      display: inline-block;
      padding: 0.75rem 1.5rem;
      background: var(--accent);
      color: #fff;
      border-radius: 4px;
      font-weight: bold;
    }

    .recipe-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem;
      max-width: var(--max);
      margin: 2rem auto;
      padding: 0 1rem;
    }

    .recipe-card {
      border: 1px solid #eee;
      border-radius: 8px;
      padding: 1rem;
      background: #fff;
    }

    .recipe-card img {
      width: 100%;
      border-radius: 8px;
      height: 200px;
      object-fit: cover;
    }

    .recipe-card h3 {
      margin-top: 0.75rem;
      font-size: 1.2rem;
    }

    .recipe-card p {
      margin: 0.25rem 0;
    }

    .recipe-card .actions {
      margin-top: 1rem;
      display: flex;
      justify-content: space-between;
    }

    .recipe-card form {
      display: inline;
    }

    .add-new {
      text-align: center;
      margin: 2rem 0;
    }

    .subscribe {
      background: #f5f5f5;
      padding: 3rem 1rem;
      text-align: center;
    }

    .site-footer {
      text-align: center;
      padding: 2rem 1rem;
      font-size: 0.9rem;
      border-top: 1px solid #ddd;
    }
  </style>
</head>
<body>

  <header class="site-header">
    <h1>Restaurant Reviews</h1>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/auth/sign-out">Sign Out</a></li>
      </ul>
    </nav>
  </header>

  <section class="hero">
    <h2>Welcome, <%= user?.email || 'Guest' %>!</h2>
    <a href="#restaurants" class="btn">View Your Reviews</a>
  </section>

  <div class="add-new">
    <a href="/users/<%= user?._id %>/restaurant/new" class="btn">+ Add New Restaurant</a>
  </div>

  <main id="restaurants" class="recipe-grid">
    <% if (!restaurants || restaurants.length === 0) { %>
      <p style="text-align:center; width: 100%;">No restaurant reviews yet. Start by adding one!</p>
    <% } else { %>
      <% restaurants.forEach(restaurant => { %>
        <article class="recipe-card">
          <img src="https://source.unsplash.com/600x400/?restaurant,food,<%= encodeURIComponent(restaurant.name) %>" alt="<%= restaurant.name %>">
          <h3><%= restaurant.name %></h3>
          <p><strong>Cuisine:</strong> <%= restaurant.cuisine || 'N/A' %></p>
          <p><strong>Rating:</strong> <%= restaurant.rating || 'N/A' %> / 5</p>
          <p><%= restaurant.review || '' %></p>

          <div class="actions">
            <a href="/users/<%= user._id %>/restaurant/<%= restaurant._id %>/edit">Edit</a>

            <form action="/users/<%= user._id %>/restaurant/<%= restaurant._id %>?_method=DELETE" method="POST">
              <button type="submit" style="color: red; background: none; border: none; cursor: pointer;">Delete</button>
            </form>
          </div>
        </article>
      <% }) %>
    <% } %>
  </main>

  <section class="subscribe">
    <h3>Get Notified About New Features</h3>
    <form action="#" method="POST">
      <input type="email" placeholder="Your email" required>
      <button type="submit">Subscribe</button>
    </form>
  </section>

  <footer class="site-footer">
    <p>&copy; 2025 Restaurant Review App</p>
  </footer>

</body>
</html>


