<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Edit Restaurant</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 2rem;
      background-color: #f8f8f8;
    }
    main {
      background: #fff;
      padding: 2rem;
      border-radius: 8px;
      max-width: 600px;
      margin: auto;
    }
    a {
      color: #e07a5f;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <header>
    <h1>Restaurant Reviews</h1>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <% if (user) { %>
          <li><a href="/auth/sign-out">Sign Out</a></li>
        <% } else { %>
          <li><a href="/auth/sign-in">Sign In</a></li>
        <% } %>
      </ul>
    </nav>
  </header>

  <main>
    <h2>Edit Restaurant</h2>

    <%- include('_form', {
      formAction: `/users/${userId}/restaurant/${restaurant._id}`,
      formMethod: 'PUT',
      restaurant,
      buttonLabel: 'Update'
    }) %>

    <p><a href="/users/<%= userId %>/restaurant">← Back to All Restaurants</a></p>
  </main>
</body>
</html>


