<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Edit Restaurant</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 2rem;
      background-color: #f8f8f8;
      color: #333;
    }
    main {
      background: #fff;
      padding: 2rem;
      border-radius: 8px;
      max-width: 600px;
      margin: auto;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    header {
      max-width: 600px;
      margin: 0 auto 1.5rem;
      padding: 0 2rem;
    }
    a {
      color: #e07a5f;
      text-decoration: none;
    }
    a:hover {
      text-decoration: underline;
    }
    nav ul {
      list-style: none;
      display: flex;
      gap: 1rem;
      padding-left: 0;
    }
  </style>
</head>
<body>
  <header>
    <h1>Restaurant Reviews</h1>
    <nav>
      <ul>
        <li><a href="/">Home</a></li>
        <% if (user) { %>
          <li><a href="/auth/sign-out">Sign Out</a></li>
        <% } else { %>
          <li><a href="/auth/sign-in">Sign In</a></li>
        <% } %>
      </ul>
    </nav>
  </header>

  <main>
    <h2>Edit Restaurant</h2>

    <%- include('_form', {
      formAction: `/users/${userId || user?._id}/restaurant/${restaurant._id}`,
      formMethod: 'PUT',
      restaurant,
      buttonLabel: 'Update'
    }) %>

    <p><a href="/users/<%= userId || user?._id %>/restaurant">← Back to All Restaurants</a></p>
  </main>
</body>
</html>
